---
description: Local quality gates, conventional commits, and pre-push workflow for craft (library-and-publishing, testing-strategy)
alwaysApply: true
---

# craft quality gates

## Before commit

```bash
./scripts/quality-gate-pre-commit.sh   # or lefthook (fmt, shellcheck, doc, clippy, check)
```

## Before push / MR

```bash
./scripts/quality-gate-pre-push.sh     # check → tests → doctests → release build
```

While iterating: `./scripts/test-with-log.sh -p <crate> --test <name>` — see `cargo-shell-safety` rule.

## Standards

- **MSRV 1.98**, edition 2024, `unsafe` forbidden workspace-wide.
- **Clippy / rustc: `-D warnings`** (`RUSTFLAGS` in CI and lefthook); **`clippy::pedantic`** via `scripts/clippy-args.sh`.
- **Conventional commits**: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `test:`.
- Reference GitLab issues as `#<number>` in commits/MR descriptions.
- **Do not commit** unless the user explicitly asks.

## CI lanes (`.gitlab-ci.yml`)

- **Fast** (every MR): fmt, clippy, nextest, doctests, doc
- **Heavy** (scheduled): e2e, `craft-store-redis --ignored`, bench/soak

New `#[ignore]` integration tests must note which heavy job runs them.
