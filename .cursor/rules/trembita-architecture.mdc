---
description: Hexagonal architecture boundaries for trembita (architecture-style) — pure core, trait ports, dependency direction
globs: crates/**/*.rs
alwaysApply: false
---

# trembita architecture (architecture-style)

## Pure core (hard rule)

- `trembita-core` and `trembita-proto` must have **no I/O**: no `tokio`, sockets, disk, or wall clock.
- Consensus is `RaftInput → (state, RaftOutput)`; effects are **data**, executed by outer runtime.
- Time in core is **logical ticks** (`RaftNode::tick`), not `Instant::now()`.

## Ports & adapters

| Port | Prod (target) | Test / sim |
|------|---------------|------------|
| `RaftStorage` | `RedbStorage` | `MemoryStorage` |
| `Transport` | QUIC | `LocalNetwork` |
| `JobQueue` | `RedbJobQueue` | `InMemoryJobQueue` |
| `ActorStateStore` | `RedbActorStateStore` (backlog B-01); optional `RedisStore` | `InMemoryStore` |

- New port = trait + **≥2 implementations**. One impl → do not abstract yet.
- Adapters depend on core; **never** core → adapter.

## Where code lives

- `trembita-core` — pure Raft FSM
- `trembita-storage` / `trembita-net` — trait + adapters
- `trembita-actor` — runtime glue (`RaftDriver`, actors)
- `trembita` — public facade (`TrembitaCluster::builder`)
- `trembita-node` — reference binary only

## Anti-patterns

```rust
// ❌ I/O inside trembita-core
async fn propose() { transport.send(...).await; }

// ✅ Effects returned; runtime executes
fn propose(&mut self, cmd: Cmd) -> Vec<Output> { vec![Output::SendRpc { .. }] }
```

Do not add `domain/` / `infrastructure/` layering inside crates. Shared wire types in `trembita-proto` are intentional.
