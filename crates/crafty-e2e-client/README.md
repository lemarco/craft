# crafty-e2e-client

E2E linearizability load generator over live QUIC/mTLS (Jepsen-lite).

**Not published to crates.io** (`publish = false`). Used by `./e2e/run.sh` and CI.

## License

Dual-licensed under `MIT OR Apache-2.0`.
