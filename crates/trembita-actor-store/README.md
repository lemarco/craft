# trembita-actor-store (deprecated)

This crate name is retained for **crates.io compatibility only**. New code should depend on **[`trembita-capstore`](https://crates.io/crates/trembita-capstore)**.

All types re-export from `trembita-capstore`. Product access remains [`trembita::capstore`](https://docs.rs/trembita/latest/trembita/capstore/index.html) on the facade.
