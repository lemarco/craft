[package]
name = "craft-net"
description = "HTTP/3 (QUIC) transport with mTLS for the craft framework."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
categories.workspace = true
keywords.workspace = true

[lints]
workspace = true

[features]
default = ["dev-certs"]
# Self-signed cluster CA + node cert generation (rcgen). On by default for the
# dev profile (ADR 006); production deployments can disable it and supply certs.
dev-certs = ["dep:rcgen"]

[dependencies]
craft-proto.workspace = true
serde.workspace = true
thiserror.workspace = true
quinn = { version = "0.11", default-features = false, features = [
    "runtime-tokio",
    "rustls-ring",
    "log",
] }
rustls = { version = "0.23", default-features = false, features = [
    "ring",
    "std",
    "tls12",
    "logging",
] }
rcgen = { version = "0.14", optional = true }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }
