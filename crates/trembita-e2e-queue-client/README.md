# trembita-e2e-queue-client

E2E job queue smoke test over live QUIC/mTLS (enqueue, follower lease/ack,
leader failover).

**Not published to crates.io** (`publish = false`). Used by `./e2e/queue.sh` and CI.

## License

Dual-licensed under `MIT OR Apache-2.0`.
