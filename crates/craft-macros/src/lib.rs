//! `craft-macros` — derive macros for the craft framework.
//!
//! Will export `#[derive(StateMachine)]` (ADR 001) and `#[derive(UserActor)]`
//! (backlog Track D). Scaffold only; no macros are exported yet.
