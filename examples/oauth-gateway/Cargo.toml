# OAuth-shaped gateway demo — IdP callback → SessionIssuer (B-40).
#
#   cargo run --release
#   curl -s 'http://127.0.0.1:8395/oauth/callback?user=alice' -c /tmp/sess.txt
#   curl -s 'http://127.0.0.1:8395/me' -b /tmp/sess.txt
[package]
name = "trembita-showcase-oauth-gateway"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
trembita = { path = "../../crates/trembita", features = ["http-jobs", "gateway-auth"] }
trembita-gateway-auth = { path = "../../crates/trembita-gateway-auth" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal"] }
http = "1"
serde_json = "1"
