# One-command 3-node workflows cluster (workflows showcase).
#
#   cd dev/compose/workflows
#   docker compose up --build
#
# Trigger: http://127.0.0.1:8490/workflows/run
# Admin:    http://127.0.0.1:9480/dashboard

x-showcase-build: &showcase-build
  build:
    context: ../../..
    dockerfile: examples/workflows/Dockerfile

x-node-env: &node-env
  TREMBITA_PEERS: "1@node1:7843,2@node2:7853,3@node3:7863"
  TREMBITA_CA_CERT: /certs/ca.pem
  RUST_LOG: info,showcase=debug,trembita=info,trembita_net=warn

services:
  certgen:
    <<: *showcase-build
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        if [ ! -f /certs/ca.pem ]; then
          /app/generate.sh --ca-only --out /certs
          for id in 1 2 3; do
            /app/generate.sh --node-id "$$id" --out /certs \
              --ca /certs/ca.pem --ca-key /certs/ca.key
          done
        fi
        ls -1 /certs
    volumes:
      - certs:/certs

  node1:
    <<: *showcase-build
    depends_on:
      certgen:
        condition: service_completed_successfully
    environment:
      <<: *node-env
      TREMBITA_NODE_ID: "1"
      TREMBITA_LISTEN: 0.0.0.0:7843
      TREMBITA_ADMIN: 0.0.0.0:9480
      TREMBITA_GATEWAY: 0.0.0.0:8490
      TREMBITA_GATEWAY_WORKFLOWS: "1"
      TREMBITA_NODE_CERT: /certs/node-1.pem
      TREMBITA_NODE_KEY: /certs/node-1.key
      TREMBITA_DATA_DIR: /data
    volumes:
      - certs:/certs
      - node1-data:/data
    ports:
      - "8490:8490"
      - "9480:9480"

  node2:
    <<: *showcase-build
    depends_on:
      certgen:
        condition: service_completed_successfully
    environment:
      <<: *node-env
      TREMBITA_NODE_ID: "2"
      TREMBITA_LISTEN: 0.0.0.0:7853
      TREMBITA_ADMIN: 0.0.0.0:9481
      TREMBITA_GATEWAY: 0.0.0.0:8491
      TREMBITA_GATEWAY_WORKFLOWS: "1"
      TREMBITA_NODE_CERT: /certs/node-2.pem
      TREMBITA_NODE_KEY: /certs/node-2.key
      TREMBITA_DATA_DIR: /data
    volumes:
      - certs:/certs
      - node2-data:/data
    ports:
      - "8491:8491"
      - "9481:9481"

  node3:
    <<: *showcase-build
    depends_on:
      certgen:
        condition: service_completed_successfully
    environment:
      <<: *node-env
      TREMBITA_NODE_ID: "3"
      TREMBITA_LISTEN: 0.0.0.0:7863
      TREMBITA_ADMIN: 0.0.0.0:9482
      TREMBITA_GATEWAY: 0.0.0.0:8492
      TREMBITA_GATEWAY_WORKFLOWS: "1"
      TREMBITA_NODE_CERT: /certs/node-3.pem
      TREMBITA_NODE_KEY: /certs/node-3.key
      TREMBITA_DATA_DIR: /data
    volumes:
      - certs:/certs
      - node3-data:/data
    ports:
      - "8492:8492"
      - "9482:9482"

volumes:
  certs:
  node1-data:
  node2-data:
  node3-data:
